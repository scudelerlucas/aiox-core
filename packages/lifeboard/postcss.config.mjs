/**
 * OS-LIFEBOARD · E5 — PostCSS para Tailwind CSS 4 (CSS-first).
 * O plugin `@tailwindcss/postcss` processa `@import "tailwindcss"` + `@config`.
 */
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
